import MainHeader from "./MainHeader";
import Slider from "./Slider";

const Header = () => {
  return (
    <header>
      <MainHeader />
      <Slider />
    </header>
  );
};

export default Header;
